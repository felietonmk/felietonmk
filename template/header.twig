<div class="header-content">
	<a href="{{ site.url }}" class="logo">
		<img src="{{ url.images("logo-small.png") }}" alt="{{ site.name }}"/>
	</a>
	
	{% if article %}
		{% if id > 1 %}
			<a class="bigger" href="{{ url.to("articles/"~(id - 1)~".html") }}">&#8249;</a>
		{% endif %}
		<a href="{{ url.to("list.html") }}">{{ id }}</a>
		<a class="bigger" id="next" href="{{ url.to("articles/"~(id + 1)~".html") }}">&#8250;</a>
	{% else %}
		<a href="{{ url.to("list.html") }}" style="font-size: 1.25em;">☰</a>
	{% endif %}

	<form class="search" action="{{ url.to("search.html") }}">
		<input type="text" name="q" id="tipue_search_input" placeholder="Szukaj" pattern=".{3,}" title="Wymagane są przynajmniej 3 znaki" required>
		<button type="submit" id="search">🔎</button>
	</form>
</div>